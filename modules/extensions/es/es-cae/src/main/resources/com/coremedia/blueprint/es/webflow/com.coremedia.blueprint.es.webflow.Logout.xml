<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow
                          http://www.springframework.org/schema/webflow/spring-webflow-2.0.xsd">

  <on-start>
    <set name="flowScope.nextUrl" value="webflowUrlHelper.getLogoutUrl(flowRequestContext)"/>
  </on-start>

  <decision-state id="isLoggedIn">
    <if test="loginHelper.isLoggedIn()" then="bpSuccess" else="bpLogout"/>
  </decision-state>


  <view-state id="bpLogout" view="logout">
    <transition on="submit" to="bpSuccess"/>
  </view-state>

  <end-state id="bpSuccess" view="externalRedirect:contextRelative:/j_spring_security_logout?spring-security-redirect=#{nextUrl}" />

</flow>