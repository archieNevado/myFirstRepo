query getVideoById($id: String!) {
  content {
    content(id: $id, type: "CMVideo") {
      ... on CMVideo {
        id
        repositoryPath
        fullyQualifiedUrl
        name
        type
      }
    }
  }
}
